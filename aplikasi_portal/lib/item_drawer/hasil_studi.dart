import 'package:flutter/material.dart';

class HasilStudiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Hasil Studi"),
      ),
    );
  }
}
