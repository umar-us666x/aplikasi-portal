import 'package:flutter/material.dart';

class MessagesFromRectorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Messages From Rector"),
      ),
    );
  }
}
